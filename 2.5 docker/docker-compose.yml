services:
  storage:
    image: redis:alpine
    volumes:
      - ./data:/data

  counter:
    image: jenycher/counter:v2.0.0
    environment:
      - PORT=3002
      - REDIS_URL=redis://storage
    ports:
      - "80:3002"
    depends_on:
      - storage